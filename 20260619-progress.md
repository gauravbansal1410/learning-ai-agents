## Progress on interview bot
- Added GitHub read node for question history (per question type)
- Parameterized prompt + history file by `type` field (product_design, 
  product_sense, execution placeholders)
- Fixed: URL/body fields must be in Expression mode for `$env` and `$json` 
  to evaluate — same issue as scraper, now confirmed pattern
- Decided: write-back happens only after debrief, not at question 
  generation. Two files: question_history (dedup, questions only) and 
  debrief_log (full review, all types combined)
- Tested end to end via `curl` — working
