## Connected slack to summarizer
- Manual Trigger → Get Slack History (fetches last 10 messages) → Aggregate (combines all messages into one) → AI Agent + Ollama (summarizes everything together)
- Connected n8n to Slack API for the first time
- Learned about OAuth scopes, webhooks, channel IDs
- Connecting n8n to a real external tool (Slack API)
- OAuth scopes — giving an app the right permissions
- Fetching data from Slack using "Get history of a channel"
- Aggregate node — combining multiple items into one
- `$json.text.join('\n')` — merging an array into readable text
