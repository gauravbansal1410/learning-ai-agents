## 20260622 - progress
- Built debrief webhook (pm-interview-debrief)
- Two GitHub PUT calls: question_history and debrief_log
- GitHub writes are non-blocking (won't break the session if they fail)
- HTML email sent via Gmail with full debrief content
- Key pattern: reference earlier nodes by name using 
  `$('NodeName').first().json` when downstream nodes break the `$json` chain
- Tested end to end via curl — all three outputs working
