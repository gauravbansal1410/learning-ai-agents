## n8n basics

`{{ $json.text }}` part is n8n's way of saying — "grab the text field from the previous node." This is called an expression — it pulls data dynamically instead of hardcoding it.
Used in the prompt message.
