## Session Goal
Complete the cloud migration of LinkedIn Job Scraper and make it production-ready with scheduling and error handling.

## What I Did

### Cloud Migration
- Imported workflow JSON into Oracle Cloud n8n (`gaurav-n8n.duckdns.org:5678`)
- Swapped Ollama (local) → Google Gemini Flash (cloud, free tier)
- Set up Gemini API key credentials in Oracle n8n
- Fixed n8n secure cookie error by adding `N8N_SECURE_COOKIE=false` env variable
- Fixed OAuth redirect URL by restarting Docker with correct `N8N_HOST`, `N8N_PROTOCOL`, `WEBHOOK_URL` env variables
- Set up free DuckDNS domain: `gaurav-n8n.duckdns.org` → Oracle IP `68.233.108.134`
- Added DuckDNS redirect URI to Google Cloud OAuth credentials
- Reconnected Gmail OAuth on Oracle n8n

### Rate Limit Fix
- Hit Gemini free tier rate limit (5 RPM for gemini-3.5-flash, 15 RPM for gemini-flash-lite)
- Tried Wait node — doesn't work (pauses once, then sends all items simultaneously)
- Tried Batch Processing in LLM Chain — doesn't actually delay between Gemini calls
- Fixed properly using **Loop Over Items** node (batch size 1) + Wait node inside loop
- This processes one post at a time with a delay — correct approach, future-proof for any model

### Error Handling
- Added `Continue (using error output)` on Apify HTTP Request node → routes to Gmail error email
- Added `Continue (using error output)` on Basic LLM Chain node → routes to Gmail error email
- Added IF node after Apify to catch empty response `[]` (actor down but no HTTP error)
  - Used `{{ Object.keys($json).length }}` to detect empty items
  - Enabled `Always Output Data` on Apify node so empty response still passes an item
  - True branch (empty) → Gmail "No posts fetched today"
  - False branch (has data) → continue normal flow
- Tested all three error scenarios — all working

### Schedule Trigger
- Added Schedule Trigger — runs daily at 9am
- Workflow published and active on Oracle Cloud

## Final Workflow Architecture
Schedule Trigger (8am daily)
↓
HTTP Request - get resume (GitHub)
↓
Extract from File
↓
HTTP Request - Apify (trigger scrape)
├── error → Gmail "Apify Error"
↓
IF - empty data check
├── true (empty) → Gmail "No posts today"
└── false (has data) ↓
Remove Duplicates
↓
Edit Fields
↓
Loop Over Items (batch size 1)
↓
Wait (15 seconds)
↓
Basic LLM Chain - Gemini scoring
├── error → Gmail "Gemini Error"
↓
Code in JavaScript
↓
Edit Fields1
↓
IF (score ≥ 7)
↓
Aggregate → Gmail Daily Digest

## Key Learnings
- Google OAuth rejects raw IP addresses as redirect URIs — needs a proper domain name
- DuckDNS provides free subdomains instantly, no credit card needed (`gaurav-n8n.duckdns.org`)
- Docker needs `rm` + `run` (not just restart) to update environment variables
- n8n Wait node pauses the whole workflow once — does NOT pace per-item calls
- n8n Batch Processing in LLM Chain node also does NOT properly delay between model calls
- **Loop Over Items** (batch size 1) + Wait node inside loop is the correct way to rate limit per-item API calls
- `Always Output Data` on HTTP Request node ensures empty responses still pass an item downstream
- `Object.keys($json).length` is the right way to detect empty items in n8n IF nodes
- Gemini free tier: gemini-flash-lite has 15 RPM, gemini-3.5-flash has only 5 RPM
